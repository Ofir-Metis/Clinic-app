FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package.json yarn.lock ./
COPY services/settings-service/package.json ./services/settings-service/

# Install dependencies
RUN yarn install --frozen-lockfile --production

# Copy built common library
COPY libs/common/dist ./libs/common/dist
COPY libs/common/package.json ./libs/common/

# Copy built service
COPY services/settings-service/dist ./services/settings-service/dist

# Create symlink to common library
RUN cd services/settings-service && \
    mkdir -p node_modules/@clinic && \
    ln -sf ../../../libs/common node_modules/@clinic/common

WORKDIR /app/services/settings-service

# Install wget for health checks
RUN apk add --no-cache wget

EXPOSE 3008

CMD ["node", "dist/main.js"]
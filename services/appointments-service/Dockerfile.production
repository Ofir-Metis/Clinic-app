FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package.json yarn.lock ./
COPY services/appointments-service/package.json ./services/appointments-service/

# Install dependencies
RUN yarn install --frozen-lockfile --production

# Copy built common library
COPY libs/common/dist ./libs/common/dist
COPY libs/common/package.json ./libs/common/

# Copy built service
COPY services/appointments-service/dist ./services/appointments-service/dist

# Create symlink to common library
RUN cd services/appointments-service && \
    mkdir -p node_modules/@clinic && \
    ln -sf ../../../libs/common node_modules/@clinic/common

WORKDIR /app/services/appointments-service

# Install wget for health checks
RUN apk add --no-cache wget

EXPOSE 3002

CMD ["node", "dist/main.js"]
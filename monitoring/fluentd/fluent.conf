# Fluentd configuration for clinic app logging
<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<filter **>
  @type record_transformer
  <record>
    hostname ${hostname}
    environment production
  </record>
</filter>

<match **>
  @type stdout
  @log_level info
</match>